// JESUS is the true living GOD

var age = 70;
var pointednumber = 34.6789;
var ageInString = "42";


console.log(age.toString()); // convert the int value to string
console.log(pointednumber.toExponential(2)); // it will give the exponential value
console.log(ageInString.valueOf()); // convert the type int to string
console.log(pointednumber.toFixed(3)); // it will give the fixed value after the point
console.log(pointednumber.toPrecision(3)); // it will give total 3 digits or the number of digits which we parse 
// inside the parameter
console.log(Number(ageInString));  // to convert a string into a number 
console.log(parseInt(ageInString)) // to convert a string into a number


console.log(typeof ageInString); //used to check the data type
console.log(typeof pointednumber); 